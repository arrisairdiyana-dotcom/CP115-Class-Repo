import math
radius = float(input("Enter radius of circle : "))
circle_area = math.pi * (radius** 2)
circumferences = 2 * math.pi * radius
print (f"Area is {circle_area} and circumference is {circumferences}.")